<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp"
    android:background="#FFF8E1">

    <!-- Mensaje superior -->
    <TextView
        android:id="@+id/tragediaPollo"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentTop="true"
        android:layout_marginTop="32dp"
        android:fontFamily="sans-serif-medium"
        android:text="@string/han_decapitado_a_pepe_ay_dale"
        android:textAlignment="center"
        android:textColor="#E65100"
        android:textSize="22sp" />

    <!-- Torso del pollo (pieza inferior) -->
    <ImageView
        android:id="@+id/torsoPollo"
        android:layout_width="180dp"
        android:layout_height="180dp"
        android:layout_centerInParent="true"
        android:src="@drawable/torso"
        android:contentDescription="Torso del pollo" />

    <!-- Cabeza del pollo (pieza superior) -->
    <ImageView
        android:id="@+id/cabezaPepe"
        android:layout_width="150dp"
        android:layout_height="130dp"
        android:layout_above="@id/torsoPollo"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:src="@drawable/cabezapepe"
        android:contentDescription="Cabeza del pollo" />

</RelativeLayout>
